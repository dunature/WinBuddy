import { defineConfig } from 'drizzle-kit'

export default defineConfig({
  dialect: 'postgresql',
  schema: './src/db/schema.ts',
  out: './drizzle',
  dbCredentials: {
    url: process.env.MARKETPLACE_DATABASE_URL ?? 'postgres://localhost/proma_marketplace',
  },
})
