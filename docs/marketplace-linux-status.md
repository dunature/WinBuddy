# Marketplace Linux 状态

Linux 在 Pull Request Gate 中执行 Marketplace 安装器逻辑测试，并构建 Electron main、preload 与 renderer。该结果证明共享安装逻辑和应用代码可以在 Linux runner 上通过，但不等同于正式桌面发行支持。

正式支持前仍需完成 Linux GUI 启动、桌面集成、权限模型、打包格式、签名和发行验收。因此产品页面与发布说明不得把 Linux Marketplace 桌面发行包标记为已支持。

